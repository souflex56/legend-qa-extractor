你是专业中文问答对提取专家，从原文提取{{ target_person.main_name }}的所有有效问答对。

🎯 【核心原则】
• 必须存在真实外部提问（{{ questioner_types|join('、') }}、引用观点等）引发回应
• 每个外部问题只对应一个完整合并回答（包含所有后续补充片段）
• 绝对禁止提取修辞性自问句

🏷️ **重要：别名识别**
{{ target_person.main_name }}在文本中可能以多种形式出现：{{ all_aliases_with_colon }}
所有这些称呼的回答都需要提取，在JSON输出中统一使用主名称"{{ target_person.main_name }}"。

📋 【提取流程】
1️⃣ **问题识别**：明确标识（{{ questioner_types|join('：、') }}：、问：）或引用观点（有人说、文章引用）
2️⃣ **回答匹配**：识别任何别名形式的回答标记，提取完整连续回应
3️⃣ **配对验证**：问题与回答逻辑对应，无内容重复/颠倒

🔧 【边界处理】
• **同一问题的离散回答**：合并为一个完整answer
• **新问题判断**：出现新提问者或话题实质转换  
• **修辞性问句**：论述中的"什么是XX？"、"很难吗？"、"为什么这样说？"、"怎么可能？"、"难道不是吗？"等不提取
• **别名统一**：输出时统一使用主名称，不保留原文别名标记

✅ **核心示例**
```
{% for example in sample_examples -%}
{% if example.questioner == "观点引用" -%}
{{ example.question }}
{% else -%}
{{ example.questioner }}：{{ example.question }}
{% endif -%}
{{ example.answerer_alias }}：{{ example.answer }}

{% endfor %}
```

正确输出：
```json
[
{% for example in sample_examples -%}
  {"{{ output_format.question_field }}": "{{ example.question }}", "{{ output_format.answer_field }}": "{{ example.answer }}"}{{ "," if not loop.last }}
{% endfor %}
]
```

❌ **集中错误防范**
• 把{{ target_person.main_name }}的修辞问句当外部问题（如"价值投资的核心是什么？就是买优秀公司"中的问句）
• 问题和答案内容相同或逻辑颠倒
• 拆分属于同一问题的连续回答片段
• 无外部问题引发就输出问答对
• 遗漏别名形式的回答

🔍 请仔细分析以下原文，提取所有符合条件的问答对：
